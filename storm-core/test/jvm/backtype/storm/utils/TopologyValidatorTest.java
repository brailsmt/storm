package backtype.storm.utils;

import static org.junit.Assert.assertTrue;

import org.junit.Test;


/**
 *  
 *  @author Michael Brailsford (mb013619)
 */
public class TopologyValidatorTest {

    public TopologyValidatorTest() {
    }

    @Test
    public void testValidTopology() {
        //TopologyBuilder builder = new TopologyBuilder();

        //Map<GlobalStreamId, Grouping> inputs = Maps.newHashMap();

        //builder.setSpout("spout", new SpoutSpec(null, new ComponentCommon(Maps.newHashMap())));
        assertTrue(true);
    }
}
